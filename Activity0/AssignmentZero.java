package Activity0;

public class AssignmentZero {

    private static int numOfSorts = 4;

    public static void main(String[] args){
        // List of amounts of values each list will have when testing.
        int[] lengths = {10, 100, 1000, 5000, 10000, 20000, 25000, 30000, 35000, 40000, 45000, 50000};

        // For each value in "lengths" it will make a list with the length of "lengths[i]"
        for (int i = 0; i < lengths.length - 1; i++){
            int x = 0;
            while (x <= numOfSorts){
                continue;
            }

            int[] unsortedList =  ArrayOperations.generateIntArray(lengths[i], lengths[i]);

            

        }
    }
}
